apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: bank-customer-tools
  namespace: agentgateway-system
spec:
  mcp:
    targets:
    - name: bank-customer-tools
      static:
        host: bank-customer-tools.bank-wiki.svc.cluster.local
        port: 8081
        path: /mcp
        protocol: StreamableHTTP
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: bank-customer-tools
  namespace: agentgateway-system
spec:
  parentRefs:
  - name: agentgateway-proxy
    namespace: agentgateway-system
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /mcp/customer
    backendRefs:
    - name: bank-customer-tools
      namespace: agentgateway-system
      group: agentgateway.dev
      kind: AgentgatewayBackend
---
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: bank-policy-tools
  namespace: agentgateway-system
spec:
  mcp:
    targets:
    - name: bank-policy-tools
      static:
        host: bank-policy-tools.bank-wiki.svc.cluster.local
        port: 8082
        path: /mcp
        protocol: StreamableHTTP
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: bank-policy-tools
  namespace: agentgateway-system
spec:
  parentRefs:
  - name: agentgateway-proxy
    namespace: agentgateway-system
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /mcp/policy
    backendRefs:
    - name: bank-policy-tools
      namespace: agentgateway-system
      group: agentgateway.dev
      kind: AgentgatewayBackend
---
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: bank-transaction-tools
  namespace: agentgateway-system
spec:
  mcp:
    targets:
    - name: bank-transaction-tools
      static:
        host: bank-transaction-tools.bank-wiki.svc.cluster.local
        port: 8083
        path: /mcp
        protocol: StreamableHTTP
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: bank-transaction-tools
  namespace: agentgateway-system
spec:
  parentRefs:
  - name: agentgateway-proxy
    namespace: agentgateway-system
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /mcp/transaction
    backendRefs:
    - name: bank-transaction-tools
      namespace: agentgateway-system
      group: agentgateway.dev
      kind: AgentgatewayBackend
---
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: bank-status-tools
  namespace: agentgateway-system
spec:
  mcp:
    targets:
    - name: bank-status-tools
      static:
        host: bank-status-tools.bank-wiki.svc.cluster.local
        port: 8085
        path: /mcp
        protocol: StreamableHTTP
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: bank-status-tools
  namespace: agentgateway-system
spec:
  parentRefs:
  - name: agentgateway-proxy
    namespace: agentgateway-system
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /mcp/status
    backendRefs:
    - name: bank-status-tools
      namespace: agentgateway-system
      group: agentgateway.dev
      kind: AgentgatewayBackend
---
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: bank-incident-tools
  namespace: agentgateway-system
spec:
  mcp:
    targets:
    - name: bank-incident-tools
      static:
        host: bank-incident-tools.bank-wiki.svc.cluster.local
        port: 8086
        path: /mcp
        protocol: StreamableHTTP
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: bank-incident-tools
  namespace: agentgateway-system
spec:
  parentRefs:
  - name: agentgateway-proxy
    namespace: agentgateway-system
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /mcp/incident
    backendRefs:
    - name: bank-incident-tools
      namespace: agentgateway-system
      group: agentgateway.dev
      kind: AgentgatewayBackend
---
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: hybrid-infra-tools
  namespace: agentgateway-system
spec:
  mcp:
    targets:
    - name: hybrid-infra-tools
      static:
        host: hybrid-infra-tools.bank-wiki.svc.cluster.local
        port: 8087
        path: /mcp
        protocol: StreamableHTTP
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hybrid-infra-tools
  namespace: agentgateway-system
spec:
  parentRefs:
  - name: agentgateway-proxy
    namespace: agentgateway-system
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /mcp/hybrid-infra
    backendRefs:
    - name: hybrid-infra-tools
      namespace: agentgateway-system
      group: agentgateway.dev
      kind: AgentgatewayBackend
