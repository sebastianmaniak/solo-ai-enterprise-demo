apiVersion: apps/v1
kind: Deployment
metadata:
  name: bank-policy-tools
  namespace: bank-wiki
  labels:
    app: bank-policy-tools
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bank-policy-tools
  template:
    metadata:
      labels:
        app: bank-policy-tools
    spec:
      containers:
      - name: policy-tools
        image: bank-policy-tools:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8082
        env:
        - name: WIKI_SERVER_URL
          value: "http://bank-wiki-server.bank-wiki.svc.cluster.local:8080"
        readinessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 2
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: bank-policy-tools
  namespace: bank-wiki
spec:
  selector:
    app: bank-policy-tools
  ports:
  - port: 8082
    targetPort: 8082
