apiVersion: apps/v1
kind: Deployment
metadata:
  name: bank-status-tools
  namespace: bank-wiki
  labels:
    app: bank-status-tools
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bank-status-tools
  template:
    metadata:
      labels:
        app: bank-status-tools
    spec:
      containers:
      - name: status-tools
        image: bank-status-tools:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8085
        readinessProbe:
          httpGet:
            path: /health
            port: 8085
          initialDelaySeconds: 2
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: bank-status-tools
  namespace: bank-wiki
spec:
  selector:
    app: bank-status-tools
  ports:
  - port: 8085
    targetPort: 8085
