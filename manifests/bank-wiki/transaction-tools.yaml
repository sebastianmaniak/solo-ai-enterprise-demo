apiVersion: apps/v1
kind: Deployment
metadata:
  name: bank-transaction-tools
  namespace: bank-wiki
  labels:
    app: bank-transaction-tools
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bank-transaction-tools
  template:
    metadata:
      labels:
        app: bank-transaction-tools
    spec:
      containers:
      - name: transaction-tools
        image: bank-transaction-tools:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8083
        env:
        - name: WIKI_SERVER_URL
          value: "http://bank-wiki-server.bank-wiki.svc.cluster.local:8080"
        readinessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 2
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: bank-transaction-tools
  namespace: bank-wiki
spec:
  selector:
    app: bank-transaction-tools
  ports:
  - port: 8083
    targetPort: 8083
